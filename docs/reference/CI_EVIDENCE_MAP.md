# CI evidence map

This register assigns each delivery contract to one authoritative GitHub Actions job. Required pull-request checks use the stable job names below; scheduled checks are release-freshness evidence and do not masquerade as commit checks.

| Contract | Authoritative workflow / job | Trigger tier | Owning paths |
| --- | --- | --- | --- |
| Formatting and repository evidence | `lint` / `Lint (ruff)` | Required PR/push | Python, tests, SQL manifests, docs contracts |
| ETL pure behavior and layer boundaries | `etl-unit` / `ETL unit tests (Python 3.11)` | Required PR/push | `src`, ETL tests, migrations |
| API contract, models, and query behavior | `api-unit` / `API unit tests (Python 3.11)` | Required PR/push | `apps/api` (routers, schemas, serving/discovery/observation-dispatch registries, the neutral observation resource, the analysis compatibility policy, authentication and API-owned saved-analysis storage, versioning), SQL query builders, and the reviewed OpenAPI contract snapshot |
| Evidence packets and the request-body bound (API-068–API-072, DB-027, WEB-031, ADR-0004) | ride `api-unit`, `api-integration`, and `frontend` above | Required PR/push | `apps/api/middleware.py` (`RequestBodyLimitMiddleware`), `apps/api/schemas/evidence_packet.py`, `apps/api/services/evidence_packet_service.py`, `apps/api/routers/evidence_packets.py`, `sql/bootstrap/002_app_api.sql`, `tests/unit/api/test_evidence_packets.py`, `tests/integration/api/test_evidence_packet_contract.py`, `apps/web/lib/evidencePackets.ts`, `apps/web/components/EvidencePacketBuilder.tsx`, `apps/web/components/ComposedArticle.tsx`, `tests/frontend/unit/evidence-packet-account.test.js`, `tests/frontend/browser/evidence-packet-account.spec.js` |
| Geography-grain vocabulary (DB-028, API-073) | ride `api-unit`, `postgres-integration`, and `api-integration` above | Required PR/push | `sql/migrations/018_geo_grain_vocabulary.sql`, `src/data_ingestion_toolbox/census_acs/gold_census/DDL/publisher.sql`, `src/data_ingestion_toolbox/census_pep/gold_pep/DDL/gold_pep.sql`, `apps/api/registry.py`, `apps/api/services/neutral_observations_service.py`, `tests/unit/api/test_serving_registry.py`, `tests/unit/api/test_neutral_observations.py`, `tests/integration/api/test_catalog_serving_agreement.py` |
| Web visualization coverage (API-138, API-139, WEB-103) | ride `api-unit` and `frontend` above | Required PR/push | `tests/support/viz_coverage.py` (the reviewed matrix of every presentation the web offers against every registered source), `tests/support/source_grains.py`, `tests/support/regenerate_viz_coverage.py`, `tests/fixtures/api/viz_coverage.json`, `tests/unit/api/test_viz_coverage.py`, `tests/frontend/unit/viz-coverage.test.js`, `apps/api/registry.py` (`ANALYSIS_NEUTRAL_PATHS`), `apps/api/schemas/catalog.py` and `apps/api/services/catalog_service.py` (`publishes_aligned_reduction`), `apps/web/lib/explorerSources.ts`, `apps/web/lib/workbench.ts`, `apps/web/components/WorkbenchPage.tsx`. Two halves of one claim: the API declares what each screen needs, and the client offers exactly what is declared. The live half rides `frontend-smoke` and `live-deployment-smoke` below (WEB-104) |
| Overall and changed executable coverage | `coverage` / `Unit + database coverage gates (Python 3.11)` | Required PR/push | Application Python and database replay tests |
| The API's locked dependency set (ENV-022) | ride `lint`/`package-api` above; refreshed by `api-lock-refresh` / `Re-resolve the API dependency lock` | Required PR/push for the lock's contents; the **refresh is not a required check** | `requirements/api.lock.txt` (the reviewed resolution), `tools/lock/refresh_api_lock.py`, `tests/unit/tooling/test_api_lock.py`, `infra/docker/Dockerfile.api`, `.github/workflows/package-api.yml`, `Makefile` (`lock-api`, `bootstrap-python`). The lock is what makes the image reproducible; the scheduled refresh is release-freshness evidence, and a red run there means the index moved, not that the repository is broken. It proposes a diff as an artifact and never merges one -- this repository uses first-party actions only |
| Runtime package contents and clean install | `package-api` / `Package build and install smoke (Python 3.11)` | Required PR/push | package metadata, runtime SQL, manifests |
| Capture history survives a reset (ADR-0006, DAG-019, DB-046, DB-047) | `dag-parse` / `DAG parse tests (Airflow 2.9.3 + Python 3.11)` and `postgres-integration` / `Warehouse integration (PostGIS 16 + Python 3.11)` | Required PR/push | `src/data_ingestion_toolbox/capture_export.py` (export, verification, restore), `dags/raw_capture_export_dag.py`, `tests/unit/shared/test_capture_export.py`, `tests/dags/test_raw_capture_export_dag.py`, `tests/integration/database/test_capture_export_round_trip.py`, `docs/decisions/0006-capture-history-survives-a-reset.md`, `docs/reference/BETA_RESET_REINGESTION.md` §2 and §4. `raw_capture` is the one layer a reset cannot reproduce, because providers do not serve their past. The round trip is graded rather than described, and the restore is graded with the append-only triggers in place -- a load that needed them off would not be restoring the property it claims to |
| Serving-table vacuum and statistics (DB-048, DEPLOY-010) | ride `coverage`/`postgres-integration`/`lint` above | Required PR/push | The `ALTER TABLE ... SET (autovacuum_*)` blocks in `gold_acs.sql`, `gold_bls.sql` and `gold_fred.sql`; `src/data_ingestion_toolbox/utility/gold_schema.py` (`_analyze_after_chunk`) and `serving_reserve.py` (`latest_table`); `tests/unit/shared/test_forced_full_reserve.py`, `tests/integration/database/test_forced_full_reserve.py`, `tests/unit/deployment/test_stack_configuration_contracts.py`, `infra/docker/docker-compose.yml` (`shm_size`). The PEP gold relations are views and carry no storage parameters; the external stack composes no database to size. Both absences are asserted rather than assumed |
| Ordered empty warehouse bootstrap, rerun, replay | `postgres-integration` / `Warehouse integration (PostGIS 16 + Python 3.11)` | Required PR/push | source DDL, migrations, bootstrap manifest, database tests |
| The shared geography guard (DAG-020) | ride `dag-parse` and `coverage` above; the refusal rides `postgres-integration` | Required PR/push | `src/data_ingestion_toolbox/silver_ref/geography_guard.py` (the one copy of the thresholds), all six `dags/*_ingest_dag.py` guard tasks, `tests/unit/shared/test_shared_geography_guard.py`, `tests/dags/test_shared_geography_guard.py`, `tests/integration/database/test_shared_geography_guard.py`, `docs/reference/BETA_RESET_REINGESTION.md` §5. The DAG tier is where "the DAG calls the helper" can be checked at all, because the callables are reached through the DagBag |
| Airflow parsing and task topology | `dag-parse` / `DAG parse tests (Airflow 2.9.3 + Python 3.11)` | Required PR/push | DAGs, orchestration code, bootstrap manifest |
| Linux scheduler compatibility | `scheduler-image` / `DAG suite in scheduler image` | Required PR/push | scheduler image, DAGs, ETL package, SQL |
| Accessibility auditor (WEB-111) | ride `frontend` above | Required PR/push | `tests/frontend/browser/accessibility-audit.spec.js` runs `@axe-core/playwright` at WCAG 2.1 AA on every route it reads from `apps/web/app`, so a route added later is audited because it exists rather than because someone added it to a list. Findings that are not this application's to fix are an allowlist of `(rule, selector, reason)` triples, never a disabled rule: disabling one hides every future instance including the ones that are ours. `tests/frontend/browser/accessibility-operations.spec.js` keeps the repository's own hand-written rules beside it |
| The browser's own reports (WEB-114) | ride `frontend` above | Required PR/push | `apps/web/lib/clientReport.ts` (the closed shape, the builders and the transport), `apps/web/app/client-report/route.ts` (the sink), `apps/web/components/ClientReporters.tsx` (four lines of wiring in the root layout), `apps/web/middleware.ts` (`report-uri`/`report-to` and the `Reporting-Endpoints` header), `apps/web/next.config.mjs` (`NEXT_PUBLIC_BUILD_ID`), `tests/frontend/unit/client-report*.test.js`, `tests/frontend/browser/client-reporting.spec.js`. The sink is same-origin because `connect-src 'self'` is not widened to gain a log line. The line in a deployed container's log is DEPLOY-011 below, which is where the `web` container is finally run |
| Browser consumer contract | `frontend` / `Frontend lint, typecheck, unit, build, and browser` | Required PR/push | web, `apps/api` response schemas, gold contracts, semantic migrations. The browser suite serves **the production build this job just made** (`next start`), not a development server compiling each route on first request: that race made one spec fail on a push that changed nothing on its route, and it meant `check:csp` graded a build the suite then did not exercise (WEB-068) |
| Route bundle budgets and the map split (WEB-113) | ride `frontend` above | Required PR/push | `apps/web/scripts/check-bundle-budget.mjs` and `apps/web/scripts/bundle-budgets.json` measure each route's JavaScript from the production build manifest `npm run build` just wrote, so the budget grades the artifact that shipped rather than an estimate from source. `apps/web/components/useMapLibre.ts` reaches MapLibre through `import()` and `apps/web/components/ComparisonWorkspace.tsx` reaches the comparison map through `next/dynamic`; `tests/frontend/browser/bundle-split.spec.js` checks at runtime that a selection with no map fetches no further JavaScript. A route with no declared budget fails rather than passing silently -- a new route nobody set a threshold for is the one that grows unnoticed |
| The warehouse manifest ledger (DB-049, DQ-SHARED-004) | `postgres-integration` | Required PR/push | `src/data_ingestion_toolbox/utility/warehouse_manifest.py` (the reader, the applier and the comparison), `scripts/apply_warehouse_manifest.py` (the operator entry point, and what `BETA_RESET_REINGESTION.md` §3 now runs), `src/data_ingestion_toolbox/quality/reconciliation.py` (`verify_manifest_ledger`), `tests/integration/database/test_warehouse_bootstrap.py`, `tests/integration/database/test_raw_schema.py`, `tests/unit/quality/test_rule_automation.py`, `tests/unit/shared/test_warehouse_manifest.py`. The tier's own warehouse is built by the same applier a deployment runs, so the ledger the rule reads here is the ledger a deployment carries rather than a fixture's idea of one |
| The deployed API's request log (API-143, DEPLOY-009) | `frontend-smoke` / `Frontend live-stack smoke` | Required PR/push | `apps/api/logging.py` (the one configuration `create_app` applies), `apps/api/telemetry.py` (the completion line itself), `src/data_ingestion_toolbox/config.py` (`API_LOG_LEVEL`), `infra/docker/docker-compose.yml` and `docker-compose.external.yml` (the container receives it), `infra/docker/stack.env.example` and `stack.external.env.example`, `tests/unit/api/test_operational_hardening.py`. The assertion runs here rather than in `deployment-smoke` because this is the only job that starts the real API image: `docker-compose.test.yml`'s `api` service is an nginx stub, and a stub writes no completion line. The step probes `/health` with an id it chose and greps the container's log for that id |
| The deployed web container's client reports (WEB-114, DEPLOY-011) | `frontend-smoke` / `Frontend live-stack smoke` | Required PR/push | `infra/docker/docker-compose.smoke.yml` (the `web` service this job now starts), `infra/docker/Dockerfile.web`, `apps/web/app/client-report/route.ts` (the sink that writes the line), `apps/web/components/ClientReporters.tsx`, `apps/web/scripts/report-a-vital.mjs` (the one navigation, which fails if the browser sent no report). This is the only job that runs the `web` image, so it is the only place the reporting path can be seen end to end: the browser tier drives a real Chromium but against `next dev` on the runner, which is not a container and has no log to read |
| Live-stack consumer contract (WEB-027, WEB-033, WEB-104) | `frontend-smoke` / `Frontend live-stack smoke` | Required PR/push | The explorer's own discovery, capability-to-source derivation, and request building run unmocked against a deployed API, Martin, and proxy: `apps/web/lib/tiles.js`, `apps/web/lib/mapWiring`, `apps/web/lib/observationAccess`, `tests/frontend/smoke/**`, `tests/sql/frontend_smoke_seed.sql`, `tests/sql/martin_seed.sql`, `infra/docker/docker-compose.smoke.yml`, `infra/web/nginx.conf`. The seed publishes one measure per registered source, derived from each source's own `metric_publisher` view rather than hand-written, and the job runs under `SMOKE_REQUIRE_ALL_SOURCES=1`: a source dropped from the seed, or a dispatch entry that stops resolving its lineage, fails here at pull-request time. This is the tier a fixture cannot stand in for -- `frontend` above serves the shapes the client expects, and both defects it was built to catch were the client reading a correct server wrongly. `tests/frontend/smoke/viz-coverage.smoke.test.js` rides the same stack under `SMOKE_REQUIRE_ALL_VIZ=1` and `SMOKE_REQUIRE_FULL_VIZ_SAMPLE=1`: it prints the source-by-presentation coverage table and fails any cell the reviewed matrix says is served that answers nothing a reader could see (WEB-104) |
| Redis cache isolation (synthetic-app mechanics) | `redis-integration` / `API cache integration (Redis 7 + Python 3.11)` | Required PR/push | Cache middleware behavior against a synthetic app plus the deterministic middleware unit file; the production-application cache/freshness contract is owned by `e2e-performance`, which provisions the warehouse it needs |
| A request's statement budget (API-147) | ride `api-unit` and `api-integration` above | Required PR/push | `apps/api/services/contracts.py` (`session_memo`, the probe memo), `apps/api/services/neutral_observations_service.py` (`resolve_metric`'s memo and `resolve_metrics`), `src/data_ingestion_toolbox/sql/catalog_queries.py` (`build_metric_details_query`), `apps/api/services/comparison_matrix_service.py`, `apps/api/services/evidence_packet_service.py`, `tests/unit/api/test_relation_probe_budget.py`, `tests/integration/api/test_statement_budget.py`. What makes the memo safe is the session's `REPEATABLE READ` isolation, which is DB-041's and API-100's to keep true; what makes it bounded is that it lives in `Session.info` and dies with the session |
| API-over-warehouse contracts (the integration tier's `tests/integration/api`) | `api-integration` / `API integration (PostGIS 16 + Redis 7 + Python 3.11)` | Required PR/push | `tests/integration/api` — the catalog/serving agreement sweeps, the evidence-packet and saved-analysis contracts, the request snapshot, the stored-work listing, the middleware order, the content report's query (API-137), and the real-database contract, against a bootstrapped PostGIS and Redis. `postgres-integration` cannot run them: it installs Airflow, which pins SQLAlchemy 1.4 against the API's 2.x |
| Spatial configuration | `martin-unit` / `Martin deterministic contracts (Python 3.11)` | Required PR/push | Martin configuration and contracts |
| Spatial database/proxy behavior | `martin-integration` / `Martin/PostGIS/API/proxy contracts` | Required PR/push | geography, gold serving, Martin, proxy |
| Deployment startup/readiness/teardown | `deployment-smoke` / `Deployment compose smoke` | Required PR/push | application, SQL, Docker, deployment config |
| Deployment entrypoint decisions (DEPLOY-008) | ride `deployment-smoke` / `Deployment compose smoke` above | Required PR/push | `tools/deployment.py` (the only copy of the rules), `scripts/deploy_stack.py` and `scripts/deploy_stack.ps1` (two thin entrypoints over it), `infra/docker/docker-compose.yml` and `docker-compose.external.yml` (whose `${VAR:-default}` values the guard's defaults map is asserted against), `tests/unit/deployment/test_deploy_stack_decisions.py`. The refusal that stands between `airflow db migrate` and the warehouse is graded here rather than only on an operator's workstation. The same job then runs the entrypoint itself against `docker-compose.test.yml` -- containers after `up`, none after `down`, and a non-zero exit on a Compose failure -- so the execution loop is covered by CI rather than by a reviewer's workstation. That stack has no `airflow-init`, so the refusal stays covered by the unit tests rather than by the live run |
| Data-product end-to-end, per change | `e2e` / `Data-product end-to-end (PostGIS 16 + Python 3.11)` | Required PR/push | Every registered data product from capture to served API (`tests/e2e`, graded by `E2E_REQUIRE_ALL_PRODUCTS`), the source publishers, `apps/api`, the serving registry, and the migrations behind them. Split out of `e2e-performance` on 2026-09-13: that workflow has only `schedule` and `workflow_dispatch` triggers, and a scheduled run grades the default branch, so no branch and no pull request had ever received end-to-end feedback -- the geography-grain vocabulary left this tier red from the day it merged (ENV-014) |
| Deployed content and consumer contract (ENV-019, WEB-102, WEB-104, API-137) | `live-deployment-smoke` / `Live deployment smoke` | Scheduled/manual; observes the deployment, never gates a merge | The same live-stack tier `frontend-smoke` runs, pointed at the real origin (`DEPLOYMENT_SMOKE_BASE_URL`) and graded against every registered source (`SMOKE_REQUIRE_ALL_SOURCES`), plus `apps/api/services/content_health.py` and `tests/support/deployment_smoke.py`. Every other job in this register grades a stack CI built seconds earlier from seeded fixtures, so none of them can see a deployment whose warehouse is empty or half-loaded: `/health/ready` answers `SELECT 1`, and an API serving zero rows for every metric passes it while drawing a blank chart on every screen. A red run here means the deployment needs attention, not that a branch is broken, which is why it carries no push or pull-request trigger. The visualization sweep runs here too, with `SMOKE_REQUIRE_ALL_VIZ` on by default and `SMOKE_REQUIRE_FULL_VIZ_SAMPLE` opt-in: a screen the deployed API declares servable that draws nothing is the same blank-chart-no-error failure one level up from an empty warehouse |
| Live provider compatibility | `external-contract` / `Census, PEP, BLS, FRED, CDC, FBI UCR, and USDA NASS live contracts` | Scheduled/manual; fresh within 7 days for release | source adapters and provider fixtures |
| Bounded E2E and performance | `e2e-performance` / `Bounded E2E and performance evidence` (the non-slow nodes of the `tests/integration/api` files below also ride `api-integration` per change) | Scheduled/manual; fresh within 7 days for release | the resilience, performance, and Locust evidence a release records, plus the same product end-to-end nodes the `e2e` job above runs per change, the production-application cache/publication-freshness contract (`tests/integration/api/test_cache_real_services.py`), the NASS API contract (`tests/integration/api/test_usda_nass_api_contract.py`), and the real-schema saved-analysis contract (`tests/integration/api/test_saved_analysis_contract.py`) |
| Data-product E2E coverage (E2E-008–E2E-014) | `e2e` / `Data-product end-to-end (PostGIS 16 + Python 3.11)` and `e2e-performance` / `Bounded E2E and performance evidence`, plus `coverage` for the inventory unit test | Required PR/push; the release run repeats it | `tests/support/product_coverage.py`, `tests/support/warehouse_scope.py`, `tests/e2e`, source publishers and API routers |
| Warehouse data-quality contracts (DQ-001–DQ-007) | ride `etl-unit`, `coverage`, `postgres-integration`, `dag-parse`, `scheduler-image` above | Required PR/push | `src/data_ingestion_toolbox/quality`, `sql/migrations/013_data_quality_evidence.sql`, quality tests, `dags/warehouse_data_quality_dag.py` |
| Served geography vocabulary and BLS measure identity (ETL-047, ETL-048, WEB-029) | ride `etl-unit`, `postgres-integration`, and `frontend` above | Required PR/push | `src/data_ingestion_toolbox/bls/gold_bls`, the ACS and FRED reporting refreshes, `tests/unit/bls/test_measure_identity.py`, `tests/unit/shared/test_incremental_serving_contract.py`, `tests/integration/database/test_fred_silver_flow.py`, `apps/web/lib/explorerViewModel.ts`, `tests/frontend/unit/bls-measure-explorer.test.js` |
| Publisher-contract changes reaching the catalog (ARC-004) | ride `etl-unit` and `postgres-integration` above | Required PR/push | `src/data_ingestion_toolbox/glossary/harvest.py`, `dags/glossary_harvest_dag.py`, `sql/migrations/016_publisher_harvest_fingerprint.sql`, `tests/unit/shared/test_glossary_harvest_guard.py`, `tests/integration/database/test_glossary_harvest_identity_change.py` |
| Forced full serving re-serve (ETL-049) | ride `etl-unit`, `postgres-integration`, and `dag-parse` above | Required PR/push | `src/data_ingestion_toolbox/utility/gold_schema.py`, `src/data_ingestion_toolbox/utility/serving_reserve.py`, `dags/serving_full_reserve_dag.py`, the three ingestion DAGs' chunk configuration, `sql/migrations/017_serving_full_reserve_run.sql`, `tests/unit/shared/test_forced_full_reserve.py`, `tests/integration/database/test_forced_full_reserve.py` |
| Served geography vocabulary and BLS measure identity (ETL-043, ETL-044, WEB-028) | ride `etl-unit`, `postgres-integration`, and `frontend` above | Required PR/push | `src/data_ingestion_toolbox/bls/gold_bls`, the ACS and FRED reporting refreshes, `tests/unit/bls/test_measure_identity.py`, `tests/unit/shared/test_incremental_serving_contract.py`, `tests/integration/database/test_fred_silver_flow.py`, `apps/web/lib/explorerViewModel.ts`, `tests/frontend/unit/bls-measure-explorer.test.js` |
| Forced full serving re-serve (ETL-045) | ride `etl-unit`, `postgres-integration`, and `dag-parse` above | Required PR/push | `src/data_ingestion_toolbox/utility/gold_schema.py`, `src/data_ingestion_toolbox/utility/serving_reserve.py`, `dags/serving_full_reserve_dag.py`, the three ingestion DAGs' chunk configuration, `sql/migrations/016_serving_full_reserve_run.sql`, `tests/unit/shared/test_forced_full_reserve.py`, `tests/integration/database/test_forced_full_reserve.py` |
| Database tier repeatability (DB-024) | ride `postgres-integration` above | Required PR/push | `tests/conftest.py`, `tests/dags/conftest.py`, `tests/support/airflow_env.py`, `tests/integration/database/test_source_quality_checks.py`, `tests/integration/database/legacy/test_fred_metadata.py`, `tests/integration/database/test_tier_repeatability.py` |
| Catalog/serving metric-code agreement (ARC-005, DB-025, DB-026, API-067) | ride `etl-unit`, `api-unit`, `postgres-integration`, `api-integration`, and `frontend` above | Required PR/push | `src/data_ingestion_toolbox/census_acs/gold_census/DDL` (the publisher view and the serving refresh that compose one metric identity), `src/data_ingestion_toolbox/glossary/harvest.py`, `apps/api/registry.py`, `tests/unit/shared/test_catalog_serving_identity.py`, `tests/integration/api/test_catalog_serving_agreement.py`, `tests/sql/martin_seed.sql`, `tests/sql/frontend_smoke_seed.sql`, `apps/web/lib/productTemplates.ts` |
| Build-your-own analytics: the workbench (API-130–API-135, WEB-082–WEB-099) | ride `api-unit`, `coverage`, and `frontend` above | Required PR/push | The two API routes and the saved kind: `apps/api/routers/comparison.py`, `apps/api/services/comparison_service.py`, `apps/api/services/comparison_matrix_service.py`, `apps/api/services/compatibility.py`, `apps/api/services/saved_analysis_service.py`, `apps/api/schemas/analysis.py`, `apps/api/schemas/saved_analysis.py`, `apps/api/registry.py`, `tests/unit/api/test_comparison_correlation.py`, `tests/unit/api/test_comparison_matrix.py`, `tests/unit/api/test_saved_analysis.py`, and the reviewed OpenAPI snapshot. The page and its modules: `apps/web/lib/workbench.ts`, `apps/web/lib/comparison.ts`, `apps/web/lib/urlState.ts`, `apps/web/lib/observationExport.ts`, `apps/web/lib/savedAnalysis.ts`, `apps/web/components/{WorkbenchPage,LineChart,BarChart,HeatmapChart,CorrelationPanel,CorrelationMatrixChart}.tsx`, `apps/web/app/workbench/`, `tests/frontend/unit/workbench*.test.js`, `tests/frontend/browser/workbench.spec.js`, and `apps/web/scripts/bundle-budgets.json`. The browser rows (WEB-086, WEB-090, WEB-094, WEB-097, WEB-099) ride `frontend`, which serves the production build the job just made |
| Published grains derive from served rows (ARC-006) | ride `etl-unit`, `postgres-integration`, and `api-integration` above | Required PR/push | `src/data_ingestion_toolbox/fred/gold_fred/DDL/publisher.sql`, `src/data_ingestion_toolbox/bls/gold_bls/DDL/publisher.sql`, `src/data_ingestion_toolbox/census_acs/gold_census/DDL/publisher.sql`, `sql/migrations/018_geo_grain_vocabulary.sql`, `tests/unit/shared/test_catalog_serving_identity.py`, `tests/integration/database/test_fred_silver_flow.py`, `tests/integration/api/test_catalog_serving_agreement.py` |

Branch protection should require the sixteen PR/push jobs above by their displayed job names. `api-integration` joined that list on 2026-09-13 — `tests/integration/api` was run by no workflow at all, while eleven plans verified against it and three rows of this register said it rode three other jobs (ENV-015). `e2e` joined on 2026-09-13, and `frontend-smoke` on 2026-09-12: the workflow had existed since the live-stack tier was built and `TESTING_CONTRACT.md` named it as the owner of WEB-027 and WEB-033, but neither this register nor its executable mirror knew it, so deleting the workflow or renaming its job would have failed no test. A release candidate additionally records successful live-provider and bounded E2E runs no older than seven days. The million-row profile remains opt-in and is required only when a release changes bulk loading, chunking, or serving-query plans.

The executable mirror is `tests/support/ci_evidence_manifest.json`; unit validation fails when a named workflow/job disappears or an architecture-sensitive owning path loses its trigger.
