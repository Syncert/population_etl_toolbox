# Initial Martin config; pending follow-up geometry naming cleanup in gold schema.
listen_addresses: 0.0.0.0:3000

postgres:
  connection_string: postgres://analytics:analytics@analytics_postgres:5432/population_etl

tables:
  gold.dim_geo_latest:
    layer_id: counties
    geometry_column: geo_geom
    id_column: geo_id
    srid: 4326
